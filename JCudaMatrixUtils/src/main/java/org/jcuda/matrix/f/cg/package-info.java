/**
 * Implementations of CG solvers based on the matrix utility classes.
 * <br>
 * <b>Note:</b> These are basically example implementations, which are
 * partially based on the NVIDIA conjugate gradient sample. For 
 * dedicated, efficient implementations of solvers, consider using 
 * JCusolver instead. 
 */
package org.jcuda.matrix.f.cg;

